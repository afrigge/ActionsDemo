name: KESO
run-name: KESOOOO!
on: workflow_dispatch

jobs:
  KESO:
    runs-on: ubuntu-latest
    steps:
      - name: Dump runner info
        run: ${{ toJSON(runner) }}
        
      - name: Dump github info
        run: ${{ toJSON(github) }}
    
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Echo stuff
        run: echo "🙌 KESO 🙌" > $GITHUB_STEP_SUMMARY
